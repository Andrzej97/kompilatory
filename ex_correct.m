# matrix function examples:
y = eye(2);
y = eye(0 + 1 + 2);
y = eye(8 / 2);
y = ones(2);
y = ones(0 + 1 + 2);
y = ones(8 / 2);
y = zeros(2);
y = zeros(0 + 1 + 2);
y = zeros(8 / 2);

# continue and break examples:
while(1){
	continue;
	break;	
}
for i = 1:N{
	continue;
    break;
}
while(1)
	continue;
while(1)
	continue;
for i = 1:N
	break;
for i = 1:N
	break;